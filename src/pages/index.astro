---
const pageTitle = "VibeStorm - Stop losing your best prompts";
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{pageTitle}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
</head>
<body>
    <!-- Hero Section -->
    <header class="hero">
        <nav class="navbar">
            <div class="container">
                <div class="logo">VibeStorm</div>
                <div class="nav-actions">
                    <button class="theme-toggle" id="theme-toggle" aria-label="Toggle dark mode">
                        <span class="sun-icon">☀️</span>
                        <span class="moon-icon">🌙</span>
                    </button>
                    <button class="cta-button-small">Sign Up</button>
                </div>
            </div>
        </nav>

        <div class="hero-content container">
            <h1 class="hero-headline">Stop losing your best prompts</h1>
            <p class="hero-subheadline">Save, organize, share, and discover prompts that actually work</p>
            <div class="hero-cta">
                <input type="email" class="email-input" placeholder="Enter your email" id="hero-email">
                <button class="cta-button" data-signup>Sign Up</button>
            </div>
            <p class="hero-caption">Get notified when its available!</p>
        </div>
    </header>

    <!-- Benefits Section -->
    <section class="benefits">
        <div class="container">
            <div class="benefits-content">
                <div class="benefits-text">
                    <h2>Your prompts deserve better than scattered docs</h2>
                    <p>You've spent hours crafting the perfect prompt, only to lose it in a sea of Google Docs, some email, or chat histories. VibeStorm gives you a dedicated workspace to:</p>
                    <ul class="benefits-list">
                        <li>Save and categorize every prompt you create</li>
                        <li>Iterate and improve with confidence</li>
                        <li>Share with anyone and everyone</li>
                        <li>Access your prompts from anywhere</li>
                    </ul>
                </div>
                <div class="benefits-visual">
                    <div class="visual-placeholder">
                        <div class="prompt-card">
                            <div class="prompt-header">
                                <span class="prompt-tag">Marketing</span>
                                <span class="prompt-version">v3.2</span>
                            </div>
                            <div class="prompt-title">Product Description Writer</div>
                            <div class="prompt-preview">You are an expert copywriter...</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features">
        <div class="container">
            <h2 class="section-title">From prompt chaos to prompt mastery</h2>
            <p class="section-subtitle">Everything you need to manage your AI prompts in one place</p>

            <div class="features-grid">
                <div class="feature-card">
                    <div class="feature-icon">💾</div>
                    <h3>Save Everything That Works</h3>
                    <p>Finally, a place for all those "oh that was a good one" moments. Save prompts instantly and never lose your best work again.</p>
                </div>

                <div class="feature-card">
                    <div class="feature-icon">🌍</div>
                    <h3>Share What You Discover</h3>
                    <p>Found a prompt that nails it? Share it with the community or keep it private - your choice. Learn from others and contribute your wins.</p>
                </div>

                <div class="feature-card">
                    <div class="feature-icon">🔮</div>
                    <h3>More Coming Soon</h3>
                    <p>We're building organization tools, templates, analytics, and AI-powered improvements. Start saving now and unlock new features as we grow.</p>
                </div>

            </div>
        </div>
    </section>

    <!-- Social Proof / Stats
    <section class="stats">
        <div class="container">
            <div class="stats-grid">
                <div class="stat-item">
                    <div class="stat-number">10,000+</div>
                    <div class="stat-label">Prompts Created</div>
                </div>
                <div class="stat-item">
                    <div class="stat-number">2,500+</div>
                    <div class="stat-label">Active Users</div>
                </div>
                <div class="stat-item">
                    <div class="stat-number">50+</div>
                    <div class="stat-label">Teams</div>
                </div>
            </div>
        </div>
    </section>-->

    <!-- Final CTA -->
    <section class="final-cta">
        <div class="container">
            <h2 class="cta-headline">Ready to Level Up Your Prompt Game?</h2>
            <p class="cta-subheadline">Sign up the mailing list and we'll tell you when VibeStorm is ready to go!</p>
            <div class="cta-form">
                <input type="email" class="email-input" placeholder="Enter your email" id="footer-email">
                <button class="cta-button" data-signup>Sign Up</button>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <div class="footer-content">
                <div class="footer-logo">VibeStorm</div>
            </div>
            <p class="footer-copyright">&copy; <span id="current-year"></span> VibeStorm. All rights reserved.</p>
        </div>
    </footer>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            line-height: 1.5;
            color: #1a1a1a;
            background: #ffffff;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 24px;
        }

        /* Navbar */
        .navbar {
            padding: 20px 0;
            border-bottom: 1px solid #f0f0f0;
        }

        .navbar .container {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .nav-actions {
            display: flex;
            align-items: center;
            gap: 16px;
        }

        .theme-toggle {
            background: none;
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            padding: 8px 12px;
            cursor: pointer;
            font-size: 20px;
            display: flex;
            align-items: center;
            transition: all 0.3s;
        }

        .theme-toggle:hover {
            border-color: #667eea;
            transform: scale(1.05);
        }

        .theme-toggle .sun-icon {
            display: block;
        }

        .theme-toggle .moon-icon {
            display: none;
        }

        .logo {
            font-size: 24px;
            font-weight: 800;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        /* Hero */
        .hero {
            background: linear-gradient(180deg, #ffffff 0%, #f8f9ff 100%);
        }

        .hero-content {
            padding: 80px 24px 120px;
            text-align: center;
        }

        .hero-headline {
            font-size: 56px;
            font-weight: 800;
            margin-bottom: 24px;
            max-width: 900px;
            margin-left: auto;
            margin-right: auto;
            background: linear-gradient(135deg, #1a1a1a 0%, #4a4a4a 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .hero-subheadline {
            font-size: 22px;
            color: #666;
            margin-bottom: 40px;
            max-width: 700px;
            margin-left: auto;
            margin-right: auto;
        }

        .hero-cta {
            display: flex;
            gap: 12px;
            max-width: 500px;
            margin: 0 auto 16px;
        }

        .email-input {
            flex: 1;
            padding: 16px 20px;
            border: 2px solid #e0e0e0;
            border-radius: 12px;
            font-size: 16px;
            transition: all 0.3s;
        }

        .email-input:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        .cta-button {
            padding: 16px 32px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: 2px solid transparent;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            white-space: nowrap;
        }

        .cta-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(102, 126, 234, 0.3);
        }

        .cta-button-small {
            padding: 10px 24px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: 2px solid transparent;
            border-radius: 8px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
        }

        .cta-button-small:hover {
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
        }

        .hero-caption {
            color: #888;
            font-size: 14px;
        }

        /* Features */
        .features {
            padding: 120px 0;
            background: #f8f9ff;
        }

        .section-title {
            font-size: 42px;
            font-weight: 700;
            text-align: center;
            margin-bottom: 16px;
        }

        .section-subtitle {
            text-align: center;
            font-size: 18px;
            color: #666;
            margin-bottom: 60px;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 32px;
        }

        .feature-card {
            padding: 32px;
            border: 1px solid #f0f0f0;
            border-radius: 16px;
            transition: all 0.3s;
        }

        .feature-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 24px rgba(0, 0, 0, 0.08);
            border-color: #667eea;
        }

        .feature-icon {
            font-size: 40px;
            margin-bottom: 16px;
        }

        .feature-card h3 {
            font-size: 22px;
            font-weight: 600;
            margin-bottom: 12px;
        }

        .feature-card p {
            color: #666;
        }

        /* Benefits */
        .benefits {
            padding: 120px 0;
            background: white;
        }

        .benefits-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 80px;
            align-items: center;
        }

        .benefits-text h2 {
            font-size: 38px;
            font-weight: 700;
            margin-bottom: 24px;
        }

        .benefits-text p {
            font-size: 18px;
            color: #666;
            margin-bottom: 24px;
        }

        .benefits-list {
            list-style: none;
        }

        .benefits-list li {
            padding: 12px 0 12px 32px;
            position: relative;
            font-size: 16px;
            color: #444;
        }

        .benefits-list li:before {
            content: "✓";
            position: absolute;
            left: 0;
            color: #667eea;
            font-weight: bold;
            font-size: 18px;
        }

        .visual-placeholder {
            background: white;
            border-radius: 16px;
            padding: 40px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
        }

        .prompt-card {
            background: linear-gradient(135deg, #f8f9ff 0%, #ffffff 100%);
            border: 1px solid #e0e0e0;
            border-radius: 12px;
            padding: 24px;
        }

        .prompt-header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 16px;
        }

        .prompt-tag {
            background: #667eea;
            color: white;
            padding: 4px 12px;
            border-radius: 6px;
            font-size: 12px;
            font-weight: 600;
        }

        .prompt-version {
            color: #888;
            font-size: 12px;
        }

        .prompt-title {
            font-size: 18px;
            font-weight: 600;
            margin-bottom: 12px;
        }

        .prompt-preview {
            color: #888;
            font-size: 14px;
            font-style: italic;
        }

        /* Stats */
        .stats {
            padding: 80px 0;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 60px;
            text-align: center;
        }

        .stat-number {
            font-size: 48px;
            font-weight: 800;
            color: white;
            margin-bottom: 8px;
        }

        .stat-label {
            font-size: 16px;
            color: rgba(255, 255, 255, 0.9);
        }

        /* Final CTA */
        .final-cta {
            padding: 120px 0;
            background: white;
            color: #1a1a1a;
            text-align: center;
        }

        .cta-headline {
            font-size: 42px;
            font-weight: 700;
            margin-bottom: 16px;
        }

        .cta-subheadline {
            font-size: 18px;
            margin-bottom: 40px;
            color: #666;
        }

        .cta-form {
            display: flex;
            gap: 12px;
            max-width: 500px;
            margin: 0 auto 16px;
        }

        .final-cta .cta-button {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: 2px solid transparent;
        }

        .final-cta .cta-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(102, 126, 234, 0.3);
        }

        .cta-caption {
            color: #888;
            font-size: 14px;
        }

        /* Footer */
        .footer {
            padding: 60px 0 40px;
            background: #1a1a1a;
            color: white;
        }

        .footer-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 40px;
        }

        .footer-logo {
            font-size: 20px;
            font-weight: 700;
        }

        .footer-links {
            display: flex;
            gap: 32px;
        }

        .footer-links a {
            color: #ccc;
            text-decoration: none;
            transition: color 0.3s;
        }

        .footer-links a:hover {
            color: white;
        }

        .footer-copyright {
            text-align: center;
            color: #888;
            font-size: 14px;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .hero-headline {
                font-size: 36px;
            }

            .hero-subheadline {
                font-size: 18px;
            }

            .hero-cta {
                flex-direction: column;
            }

            .features-grid {
                grid-template-columns: 1fr;
            }

            .benefits-content {
                grid-template-columns: 1fr;
                gap: 40px;
            }

            .stats-grid {
                grid-template-columns: 1fr;
                gap: 40px;
            }

            .cta-form {
                flex-direction: column;
            }

            .footer-content {
                flex-direction: column;
                gap: 24px;
            }
        }

        /* Dark Mode */
        [data-theme="dark"] {
            color-scheme: dark;
        }

        [data-theme="dark"] body {
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            color: #ffffff;
        }

        [data-theme="dark"] .navbar {
            border-bottom-color: rgba(255, 255, 255, 0.1);
        }

        [data-theme="dark"] .logo {
            background: none;
            -webkit-text-fill-color: white;
            color: white;
        }

        [data-theme="dark"] .theme-toggle {
            border-color: rgba(255, 255, 255, 0.2);
        }

        [data-theme="dark"] .theme-toggle:hover {
            border-color: #667eea;
        }

        [data-theme="dark"] .theme-toggle .sun-icon {
            display: none;
        }

        [data-theme="dark"] .theme-toggle .moon-icon {
            display: block;
        }

        [data-theme="dark"] .hero {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }

        [data-theme="dark"] .hero-headline {
            background: linear-gradient(135deg, #ffffff 0%, #f0f0f0 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        [data-theme="dark"] .hero-subheadline {
            color: rgba(255, 255, 255, 0.9);
        }

        [data-theme="dark"] .hero-caption {
            color: rgba(255, 255, 255, 0.7);
        }

        [data-theme="dark"] .email-input {
            background: rgba(255, 255, 255, 0.1);
            border-color: rgba(255, 255, 255, 0.3);
            color: white;
        }

        [data-theme="dark"] .email-input::placeholder {
            color: rgba(255, 255, 255, 0.6);
        }

        [data-theme="dark"] .email-input:focus {
            background: rgba(255, 255, 255, 0.15);
            border-color: white;
        }

        [data-theme="dark"] .cta-button {
            background: rgba(255, 255, 255, 0.15);
            color: white;
            border: 2px solid rgba(255, 255, 255, 0.3);
        }

        [data-theme="dark"] .cta-button:hover {
            background: rgba(255, 255, 255, 0.25);
            border-color: white;
        }

        [data-theme="dark"] .cta-button-small {
            background: rgba(255, 255, 255, 0.15);
            color: white;
            border: 2px solid rgba(255, 255, 255, 0.3);
        }

        [data-theme="dark"] .cta-button-small:hover {
            background: rgba(255, 255, 255, 0.25);
            border-color: white;
        }

        [data-theme="dark"] .benefits {
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
        }

        [data-theme="dark"] .benefits-text p {
            color: rgba(255, 255, 255, 0.9);
        }

        [data-theme="dark"] .benefits-list li {
            color: rgba(255, 255, 255, 0.95);
        }

        [data-theme="dark"] .benefits-list li:before {
            color: white;
        }

        [data-theme="dark"] .visual-placeholder {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
        }

        [data-theme="dark"] .prompt-card {
            background: linear-gradient(135deg, rgba(255, 255, 255, 0.15) 0%, rgba(255, 255, 255, 0.05) 100%);
            border-color: rgba(255, 255, 255, 0.2);
        }

        [data-theme="dark"] .prompt-version {
            color: rgba(255, 255, 255, 0.7);
        }

        [data-theme="dark"] .prompt-preview {
            color: rgba(255, 255, 255, 0.7);
        }

        [data-theme="dark"] .features {
            background: linear-gradient(135deg, #2d1b4e 0%, #1e1535 100%);
        }

        [data-theme="dark"] .section-subtitle {
            color: rgba(255, 255, 255, 0.8);
        }

        [data-theme="dark"] .feature-card {
            background: rgba(255, 255, 255, 0.05);
            border-color: rgba(255, 255, 255, 0.1);
        }

        [data-theme="dark"] .feature-card:hover {
            background: rgba(255, 255, 255, 0.08);
            border-color: #667eea;
        }

        [data-theme="dark"] .feature-card p {
            color: rgba(255, 255, 255, 0.8);
        }

        [data-theme="dark"] .stats {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }

        [data-theme="dark"] .final-cta {
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            color: white;
        }

        [data-theme="dark"] .cta-headline {
            color: white;
        }

        [data-theme="dark"] .cta-subheadline {
            color: rgba(255, 255, 255, 0.8);
        }

        [data-theme="dark"] .final-cta .email-input {
            background: rgba(255, 255, 255, 0.1);
            color: white;
            border-color: rgba(255, 255, 255, 0.3);
        }

        [data-theme="dark"] .final-cta .email-input::placeholder {
            color: rgba(255, 255, 255, 0.6);
        }

        [data-theme="dark"] .final-cta .email-input:focus {
            background: rgba(255, 255, 255, 0.15);
            border-color: white;
        }

        [data-theme="dark"] .final-cta .cta-button {
            background: rgba(255, 255, 255, 0.15);
            color: white;
            border: 2px solid rgba(255, 255, 255, 0.3);
        }

        [data-theme="dark"] .final-cta .cta-button:hover {
            background: rgba(255, 255, 255, 0.25);
            border-color: white;
        }

        [data-theme="dark"] .cta-caption {
            color: rgba(255, 255, 255, 0.7);
        }

        [data-theme="dark"] .footer {
            background: #0f0f1e;
        }
    </style>

    <script>
        // Set current year in footer
        const yearSpan = document.getElementById('current-year');
        if (yearSpan) {
            yearSpan.textContent = new Date().getFullYear().toString();
        }

        // Theme toggle functionality
        const themeToggle = document.getElementById('theme-toggle');
        const htmlElement = document.documentElement;

        // Check for saved theme preference or default to light mode
        const currentTheme = localStorage.getItem('theme') || 'light';
        htmlElement.setAttribute('data-theme', currentTheme);

        themeToggle?.addEventListener('click', () => {
            const currentTheme = htmlElement.getAttribute('data-theme');
            const newTheme = currentTheme === 'dark' ? 'light' : 'dark';

            htmlElement.setAttribute('data-theme', newTheme);
            localStorage.setItem('theme', newTheme);
        });

        function handleSignup() {
            const heroEmail = document.getElementById('hero-email') as HTMLInputElement;
            const footerEmail = document.getElementById('footer-email') as HTMLInputElement;
            const email = heroEmail?.value || footerEmail?.value;

            if (email && email.includes('@')) {
                alert(`Thanks for signing up! We'll send a confirmation to ${email}`);
                // In production, this would send to your backend
            } else {
                alert('Please enter a valid email address');
            }
        }

        // Attach handlers
        document.querySelectorAll('[data-signup]').forEach(button => {
            button.addEventListener('click', handleSignup);
        });

        // Smooth scroll for nav CTA
        document.querySelectorAll('.cta-button-small').forEach(button => {
            button.addEventListener('click', () => {
                document.querySelector('.final-cta')?.scrollIntoView({ behavior: 'smooth' });
            });
        });
    </script>
</body>
</html>
