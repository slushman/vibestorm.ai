---
const pageTitle = "VibeStorm - Stop losing your best prompts";
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{pageTitle}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
    <script async src="https://subscribe-forms.beehiiv.com/embed.js"></script>
</head>
<body class="font-sans leading-normal text-[#1a1a1a] bg-white dark:bg-gradient-to-br dark:from-[#1a1a2e] dark:to-[#16213e] dark:text-white">
    <!-- Hero Section -->
    <header class="bg-gradient-to-b from-white to-[#f8f9ff] dark:bg-gradient-to-br dark:from-brand-purple dark:to-brand-purple-dark">
        <nav class="py-5 border-b border-[#f0f0f0] dark:border-white/10">
            <div class="max-w-7xl mx-auto px-6 flex justify-between items-center">
                <div class="text-2xl font-extrabold bg-gradient-to-br from-brand-purple to-brand-purple-dark bg-clip-text text-transparent dark:bg-none dark:text-white">VibeStorm</div>
                <div class="flex items-center gap-4">
                    <a href="/blog" class="text-[#666] hover:text-brand-purple dark:text-white/80 dark:hover:text-white transition-colors font-semibold">Blog</a>
                    <button class="theme-toggle bg-transparent border-2 border-[#e0e0e0] dark:border-white/20 rounded-lg px-3 py-2 cursor-pointer text-xl flex items-center transition-all hover:border-brand-purple hover:scale-105 dark:hover:border-brand-purple" id="theme-toggle" aria-label="Toggle dark mode">
                        <span class="sun-icon block dark:hidden">☀️</span>
                        <span class="moon-icon hidden dark:block">🌙</span>
                    </button>
                </div>
            </div>
        </nav>

        <div class="max-w-7xl mx-auto px-6 py-20 pb-[120px] text-center">
            <h1 class="text-[56px] font-extrabold mb-6 max-w-[900px] mx-auto bg-gradient-to-br from-[#1a1a1a] to-[#4a4a4a] bg-clip-text text-transparent leading-tight dark:from-white dark:to-[#f0f0f0]">Stop losing your best prompts</h1>
            <p class="text-[22px] text-[#666] mb-10 max-w-[700px] mx-auto dark:text-white/90">Save, organize, share, and discover prompts that actually work</p>
            <div class="flex gap-3 max-w-[500px] mx-auto mb-4 flex-col md:flex-row">
                <iframe src="https://subscribe-forms.beehiiv.com/1adcc035-7aac-43af-bcf8-1286c23e2f33" class="beehiiv-embed" data-test-id="beehiiv-embed" frameborder="0" scrolling="no" style="width: 400px; height: 47px; margin: 0; border-radius: 0px 0px 0px 0px !important; background-color: transparent; box-shadow: 0 0 #0000; max-width: 100%;"></iframe>
            </div>
            <p class="text-[#888] text-sm dark:text-white/70">Get notified when its available!</p>
        </div>
    </header>

    <!-- Benefits Section -->
    <section class="py-[120px] bg-white dark:bg-gradient-to-br dark:from-[#1a1a2e] dark:to-[#16213e]">
        <div class="max-w-7xl mx-auto px-6">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-[80px] items-center">
                <div>
                    <h2 class="text-[38px] font-bold mb-6">Your prompts deserve better than scattered docs</h2>
                    <p class="text-lg text-[#666] mb-6 dark:text-white/90">You've spent hours crafting the perfect prompt, only to lose it in a sea of Google Docs, some email, or chat histories. VibeStorm gives you a dedicated workspace to:</p>
                    <ul class="list-none">
                        <li class="py-3 pl-8 relative text-base text-[#444] before:content-['✓'] before:absolute before:left-0 before:text-brand-purple before:font-bold before:text-lg dark:text-white/95 dark:before:text-white">Save and categorize every prompt you create</li>
                        <li class="py-3 pl-8 relative text-base text-[#444] before:content-['✓'] before:absolute before:left-0 before:text-brand-purple before:font-bold before:text-lg dark:text-white/95 dark:before:text-white">Iterate and improve with confidence</li>
                        <li class="py-3 pl-8 relative text-base text-[#444] before:content-['✓'] before:absolute before:left-0 before:text-brand-purple before:font-bold before:text-lg dark:text-white/95 dark:before:text-white">Share with anyone and everyone</li>
                        <li class="py-3 pl-8 relative text-base text-[#444] before:content-['✓'] before:absolute before:left-0 before:text-brand-purple before:font-bold before:text-lg dark:text-white/95 dark:before:text-white">Access your prompts from anywhere</li>
                    </ul>
                </div>
                <div class="bg-white rounded-2xl p-10 shadow-[0_20px_60px_rgba(0,0,0,0.1)] dark:bg-white/10 dark:backdrop-blur-[10px]">
                    <div class="bg-gradient-to-br from-[#f8f9ff] to-white border border-[#e0e0e0] rounded-xl p-6 dark:from-brand-purple/40 dark:to-brand-purple-dark/20 dark:border-white/20">
                        <div class="flex justify-between mb-4">
                            <span class="bg-brand-purple text-white px-3 py-1 rounded-md text-xs font-semibold">Marketing</span>
                            <span class="text-[#888] text-xs dark:text-white/70">v3.2</span>
                        </div>
                        <div class="text-lg font-semibold mb-3">Product Description Writer</div>
                        <div class="text-[#888] text-sm italic dark:text-white/70">You are an expert copywriter...</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="py-[120px] bg-[#f8f9ff] dark:bg-gradient-to-br dark:from-[#2d1b4e] dark:to-[#1e1535]">
        <div class="max-w-7xl mx-auto px-6">
            <h2 class="text-[42px] font-bold text-center mb-4">From prompt chaos to prompt mastery</h2>
            <p class="text-center text-lg text-[#666] mb-[60px] dark:text-white/80">Everything you need to manage your AI prompts in one place</p>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <div class="p-8 border border-[#f0f0f0] rounded-2xl transition-all hover:-translate-y-1 hover:shadow-[0_12px_24px_rgba(0,0,0,0.08)] hover:border-brand-purple dark:bg-white/5 dark:border-white/10 dark:hover:bg-white/8">
                    <div class="text-[40px] mb-4">💾</div>
                    <h3 class="text-[22px] font-semibold mb-3">Save Everything That Works</h3>
                    <p class="text-[#666] dark:text-white/80">Finally, a place for all those "oh that was a good one" moments. Save prompts instantly and never lose your best work again.</p>
                </div>

                <div class="p-8 border border-[#f0f0f0] rounded-2xl transition-all hover:-translate-y-1 hover:shadow-[0_12px_24px_rgba(0,0,0,0.08)] hover:border-brand-purple dark:bg-white/5 dark:border-white/10 dark:hover:bg-white/8">
                    <div class="text-[40px] mb-4">🌍</div>
                    <h3 class="text-[22px] font-semibold mb-3">Share What You Discover</h3>
                    <p class="text-[#666] dark:text-white/80">Found a prompt that nails it? Share it with the community or keep it private - your choice. Learn from others and contribute your wins.</p>
                </div>

                <div class="p-8 border border-[#f0f0f0] rounded-2xl transition-all hover:-translate-y-1 hover:shadow-[0_12px_24px_rgba(0,0,0,0.08)] hover:border-brand-purple dark:bg-white/5 dark:border-white/10 dark:hover:bg-white/8">
                    <div class="text-[40px] mb-4">🔮</div>
                    <h3 class="text-[22px] font-semibold mb-3">More Coming Soon</h3>
                    <p class="text-[#666] dark:text-white/80">We're building organization tools, templates, analytics, and AI-powered improvements. Start saving now and unlock new features as we grow.</p>
                </div>

            </div>
        </div>
    </section>

    <!-- Social Proof / Stats
    <section class="py-20 bg-gradient-to-br from-brand-purple to-brand-purple-dark text-white">
        <div class="max-w-7xl mx-auto px-6">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-[60px] text-center">
                <div>
                    <div class="text-5xl font-extrabold text-white mb-2">10,000+</div>
                    <div class="text-base text-white/90">Prompts Created</div>
                </div>
                <div>
                    <div class="text-5xl font-extrabold text-white mb-2">2,500+</div>
                    <div class="text-base text-white/90">Active Users</div>
                </div>
                <div>
                    <div class="text-5xl font-extrabold text-white mb-2">50+</div>
                    <div class="text-base text-white/90">Teams</div>
                </div>
            </div>
        </div>
    </section>-->

    <!-- Final CTA -->
    <section class="py-[120px] bg-white text-[#1a1a1a] text-center dark:bg-gradient-to-br dark:from-[#1a1a2e] dark:to-[#16213e] dark:text-white">
        <div class="max-w-7xl mx-auto px-6">
            <h2 class="text-[42px] font-bold mb-4 dark:text-white">Ready to Level Up Your Prompt Game?</h2>
            <p class="text-lg mb-10 text-[#666] dark:text-white/80">Sign up the mailing list and we'll tell you when VibeStorm is ready to go!</p>
            <div class="flex gap-3 max-w-[500px] mx-auto mb-4 flex-col md:flex-row">
                <iframe src="https://subscribe-forms.beehiiv.com/1adcc035-7aac-43af-bcf8-1286c23e2f33" class="beehiiv-embed" data-test-id="beehiiv-embed" frameborder="0" scrolling="no" style="width: 400px; height: 47px; margin: 0; border-radius: 0px 0px 0px 0px !important; background-color: transparent; box-shadow: 0 0 #0000; max-width: 100%;"></iframe>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="py-[60px] pb-[40px] bg-[#1a1a1a] text-white dark:bg-[#0f0f1e]">
        <div class="max-w-7xl mx-auto px-6">
            <div class="flex justify-between items-center mb-10">
                <div class="text-xl font-bold">VibeStorm</div>
            </div>
            <p class="text-center text-[#888] text-sm">&copy; <span id="current-year"></span> VibeStorm. All rights reserved.</p>
        </div>
    </footer>

    <script>
        // Set current year in footer
        const yearSpan = document.getElementById('current-year');
        if (yearSpan) {
            yearSpan.textContent = new Date().getFullYear().toString();
        }

        // Theme toggle functionality
        const themeToggle = document.getElementById('theme-toggle');
        const htmlElement = document.documentElement;

        // Check for saved theme preference or default to light mode
        const currentTheme = localStorage.getItem('theme') || 'light';
        if (currentTheme === 'dark') {
            htmlElement.classList.add('dark');
        }
        htmlElement.setAttribute('data-theme', currentTheme);

        themeToggle?.addEventListener('click', () => {
            const isDark = htmlElement.classList.contains('dark');
            const newTheme = isDark ? 'light' : 'dark';

            if (isDark) {
                htmlElement.classList.remove('dark');
            } else {
                htmlElement.classList.add('dark');
            }

            htmlElement.setAttribute('data-theme', newTheme);
            localStorage.setItem('theme', newTheme);
        });

        function handleSignup() {
            const heroEmail = document.getElementById('hero-email') as HTMLInputElement;
            const footerEmail = document.getElementById('footer-email') as HTMLInputElement;
            const email = heroEmail?.value || footerEmail?.value;

            if (email && email.includes('@')) {
                alert(`Thanks for signing up! We'll send a confirmation to ${email}`);
                // In production, this would send to your backend
            } else {
                alert('Please enter a valid email address');
            }
        }

        // Attach handlers
        document.querySelectorAll('[data-signup]').forEach(button => {
            button.addEventListener('click', handleSignup);
        });

        // Smooth scroll for nav CTA
        document.querySelectorAll('.cta-button-small').forEach(button => {
            button.addEventListener('click', () => {
                document.querySelector('.final-cta')?.scrollIntoView({ behavior: 'smooth' });
            });
        });
    </script>
</body>
</html>
